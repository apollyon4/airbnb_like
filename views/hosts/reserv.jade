extends ../layout

block content
  .container
    h1.page-header 예약 요청
    form(action='/hosts/#{host._id}/reserv', method='POST')
      .form-group
        label(for='checkin') Check-In
        input.form-control(type="text", class="datepicker", name='checkin', placeholder='CheckIn')
      .form-group
        label(for='checkout') Check-Out
        input.form-control(type="text", class='datepicker', name='checkout', placeholder='CheckIn')
      .form-group
        label(for='people') 숙박 인원
        input.form-control(id='people', name='people', placeholder='1명')
      div.form-actions
        a.btn.btn-default(href='javascript:window.history.back();') 뒤로 
        | 
        button.btn.btn-primary(type='submit') 예약하기 
        
  script(type='text/javascript').
    $(function() {
      $(".datepicker").datepicker();
    });
